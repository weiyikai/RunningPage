# Project dependencies
.yarn
.cache
*.pyc
node_modules
yarn-error.log

# Build directory
/build
/dist
.DS_Store
.sass-cache/
_posts/
_site/

.mypy_cache

/.vscode
.idea

config.yaml
.venv/
.pdm-python

route.png
route.svg

.env

# Garmin auto-renewal credentials & cached secrets (local only)
.garmin_credentials
.garmin_secrets

# Garmin FIT download intermediate archives (the extracted .fit/.gpx/.tcx is
# what gets committed; the .zip is only a download container)
FIT_OUT/*.zip
GPX_OUT/*.zip
